[build]
  command = "cd apps/web && npm install && npm run build"
  publish = "apps/web/.next"
  base = "."

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/_next/static/*"
  to = "/_next/static/:splat"
  status = 200